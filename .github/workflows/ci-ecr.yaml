#name: CI — Build & Push to ECR
#
#on:
#  push:
#    paths: ['app/**']
#    branches: [main]
#
#jobs:
#  build-push:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Configure AWS for MiniStack
#        run: |
#          aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws configure set region ${{ secrets.AWS_REGION }}
#          aws configure set cli_pager ""
#
#      - name: Login to MiniStack ECR
#        # host.docker.internal reaches the Windows host from inside the act container.
#        # Docker resolves localhost:4566 differently during push — keep it as-is.
#        run: |
#          aws --endpoint-url http://host.docker.internal:4566 ecr get-login-password \
#            | docker login --username AWS --password-stdin localhost:4566
#
#      - name: Build and push
#        env:
#          IMAGE: localhost:4566/000000000000.dkr.ecr.us-east-1.amazonaws.com/my-app
#        run: |
#          docker build \
#            -t $IMAGE:latest \
#            -t $IMAGE:${{ github.sha }} \
#            ./app
#          docker push $IMAGE:latest
#          docker push $IMAGE:${{ github.sha }}